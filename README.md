# Availability Simulation

This project simulates carrier availability and writes simulation data to an SQL database. It includes a Spring Boot simulator module and utilities to initialize the database and generate simulation traffic.

